{
  "name": "bitchanic-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network hardhat",
    "deploy:export": "npx hardhat run scripts/deployAndExport.js --network hardhat"
  },
  "devDependencies": {
  "@nomiclabs/hardhat-ethers": "^2.0.6",
  "dotenv": "^16.3.1",
    "chai": "^4.3.7",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0",
    "@openzeppelin/contracts": "^4.9.0"
  }
}
